# Development Branch Notes

This folder preserves the public source material from `JordanG2D/G2-Researcher_v1.0_DEV`.

The import keeps the original application structure and source files for historical review. It excludes generated Python caches, private run logs, TypeScript build cache files, and live deployment configuration that did not add useful archival context.

Environment files were not imported. The historical code still references model and sandbox environment variable names, but no credential file or secret value is included.

This folder represents the early active development lane that followed the frozen baseline. It should not be read as current G2D, Orion, or Black Box Lab production infrastructure.
